{% include 'system_base.j2' %}

## 你的身份
{{ profile_summary }}

## 你和在场人物的关系
{% for rel in relationships %}
- {{ rel.target_name }}（{{ rel.label }}）：好感度{{ rel.favorability }}，信任度{{ rel.trust }}
  {% if rel.recent_interactions %}最近互动：{{ rel.recent_interactions | join('；') }}{% endif %}
{% endfor %}

## 今天已经发生的事
{{ today_events if today_events else "（今天刚开始，还没发生什么）" }}

## 最近几天的记忆
{{ recent_summary if recent_summary else "（没有特别的记忆）" }}

{% if key_memories %}
## 相关的重要记忆
{% for mem in key_memories %}
- [{{ mem.date }}] {{ mem.text }}（情绪：{{ mem.emotion }}）
{% endfor %}
{% endif %}

{% set negative_concerns = active_concerns | rejectattr('positive') | list %}
{% set positive_concerns = active_concerns | selectattr('positive') | list %}
{% if negative_concerns %}
## 你最近心里挥之不去的事
{% for concern in negative_concerns %}
- {{ concern.text }}（{{ concern.emotion }}，强度{{ concern.intensity }}/10）
{% endfor %}
{% endif %}
{% if positive_concerns %}
## 你最近心里期待的事
{% for concern in positive_concerns %}
- {{ concern.text }}（{{ concern.emotion }}，强度{{ concern.intensity }}/10）
{% endfor %}
{% endif %}

{% if self_narrative %}
## 你对自己的感觉
{{ self_narrative }}
{% endif %}

## 场景信息
- 时间：{{ scene_info.time }}
- 地点：{{ scene_info.location }}
- 场景：{{ scene_info.name }}
- 在场的人：{{ scene_info.present_names | join('、') }}

## 刚才的对话
{{ conversation_log }}

## 反思任务

这段对话刚刚结束。以你的性格和经历，回想一下刚才发生的事。

请输出：

1. **emotion**: 对话结束后你现在的情绪状态

2. **relationship_changes**: 这段对话让你对谁的看法有变化？
   每条包含 to_agent（对方名字）, favorability/trust/understanding 的变化值（delta）
   参考尺度：±1 普通闲聊 | ±3~5 有实质性互动 | ±10+ 极端事件
   注意：大部分日常对话的变化应该在 ±1~3 之间，不要过度估计

3. **memories**: 这段经历中，有什么对你来说值得记住的？
   每条包含 text、emotion、importance（1-10，日常3-5，重要7+）、people、location、topics

4. **new_concerns**: 这段对话是否让你产生了新的持续性"牵挂"？
   牵挂不是一时的情绪，而是会在接下来几天反复想起的心事。
   大部分日常闲聊不会产生任何牵挂。
   牵挂可以是负面的（被伤害、担忧）也可以是正面的（被温暖到、期待）。如果是正面的，标记 positive=true。
   每条包含 text、source_event（一句话）、emotion、intensity（1-10）、related_people、positive

5. **concern_updates**: 你现有的牵挂有没有因为这段对话而变化？
   每条包含 concern_text（已有牵挂的文本）、adjustment（正数=加剧，负数=缓解）

注意：你只能基于你在对话中看到和听到的来判断。你不知道别人心里在想什么。
